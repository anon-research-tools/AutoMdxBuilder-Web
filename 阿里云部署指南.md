# AutoMdxBuilder Web 阿里云部署指南

## 目录
1. [服务器要求](#服务器要求)
2. [部署步骤](#部署步骤)
3. [配置说明](#配置说明)
4. [运维管理](#运维管理)
5. [常见问题](#常见问题)

---

## 服务器要求

### 最低配置
- **CPU**: 2核
- **内存**: 4GB
- **硬盘**: 40GB SSD
- **带宽**: 5Mbps
- **系统**: Ubuntu 20.04/22.04 LTS

### 推荐配置
- **CPU**: 4核
- **内存**: 8GB
- **硬盘**: 100GB SSD
- **带宽**: 10Mbps
- **系统**: Ubuntu 22.04 LTS

---

## 部署步骤

### 1. 准备阿里云ECS实例

#### 1.1 购买ECS
1. 登录阿里云控制台
2. 选择"云服务器ECS" → "实例"
3. 点击"创建实例"
4. 选择配置：
   - 地域：选择离用户最近的地域
   - 实例规格：推荐 ecs.c6.xlarge（4核8GB）
   - 镜像：Ubuntu 22.04 64位
   - 网络：默认VPC
   - 公网IP：分配
   - 安全组：允许 22, 80, 443 端口

#### 1.2 配置安全组规则
```
入方向规则：
- SSH (22/TCP) - 源：你的IP/0.0.0.0/0
- HTTP (80/TCP) - 源：0.0.0.0/0
- HTTPS (443/TCP) - 源：0.0.0.0/0
```

#### 1.3 连接到服务器
```bash
ssh root@你的服务器IP
```

---

### 2. 初始化服务器

#### 2.1 创建工作目录
```bash
mkdir -p /opt/automdxbuilder
cd /opt/automdxbuilder
```

#### 2.2 上传项目文件
在本地执行（方法1 - 使用rsync）：
```bash
# 在AutoMdxBuilder_Web目录下
rsync -avz --exclude 'venv' --exclude '__pycache__' \
    ./ root@你的服务器IP:/opt/automdxbuilder/
```

或使用SCP（方法2）：
```bash
scp -r AutoMdxBuilder_Web/* root@你的服务器IP:/opt/automdxbuilder/
```

或使用Git（方法3 - 推荐）：
```bash
# 在服务器上
cd /opt/automdxbuilder
git clone https://github.com/yourusername/AutoMdxBuilder_Web.git .
```

#### 2.3 上传AutoMdxBuilder可执行文件
```bash
# 在本地（从桌面版项目）
scp /Users/lizhouyuan/E盘/MDX製作/AutoMdxBuilder/AutoMdxBuilder \
    root@你的服务器IP:/opt/automdxbuilder/AutoMdxBuilder
```

#### 2.4 运行初始化脚本
```bash
cd /opt/automdxbuilder
chmod +x deploy/*.sh
sudo ./deploy/setup_server.sh
```

这个脚本会：
- 安装Docker和Docker Compose
- 配置防火墙
- 优化系统参数
- 创建必要的目录

---

### 3. 配置应用

#### 3.1 配置环境变量
```bash
cd /opt/automdxbuilder
cp .env.example .env
nano .env
```

修改以下配置：
```bash
# Flask配置
FLASK_ENV=production
SECRET_KEY=你的随机密钥  # 使用: openssl rand -hex 32 生成

# Celery配置
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# AutoMdxBuilder路径
AMB_PATH=/usr/local/bin/AutoMdxBuilder

# 文件保留时间（小时）
FILE_RETENTION_HOURS=24
```

#### 3.2 验证AutoMdxBuilder
```bash
chmod +x /opt/automdxbuilder/AutoMdxBuilder
/opt/automdxbuilder/AutoMdxBuilder --version
```

---

### 4. 部署应用

#### 4.1 运行部署脚本
```bash
cd /opt/automdxbuilder
./deploy/aliyun_deploy.sh
```

这个脚本会：
1. 检查环境
2. 构建Docker镜像
3. 启动所有服务
4. 显示服务状态

#### 4.2 验证部署
```bash
# 查看运行的容器
docker-compose ps

# 应该看到5个容器运行：
# - automdx_nginx (Nginx)
# - automdx_web (Flask Web)
# - automdx_celery (Celery Worker)
# - automdx_beat (Celery Beat)
# - automdx_redis (Redis)

# 查看日志
docker-compose logs -f web
```

#### 4.3 测试访问
```bash
# 获取服务器IP
curl ifconfig.me

# 在浏览器访问
http://你的服务器IP
```

---

## 配置说明

### Docker Compose服务

```yaml
服务名称          端口    说明
nginx            80      Nginx反向代理
web              8000    Flask Web应用
redis            6379    Redis缓存和消息队列
celery_worker    -       Celery异步任务处理
celery_beat      -       Celery定时任务
```

### 目录结构

```
/opt/automdxbuilder/
├── app/                    # Flask应用
├── core/                   # 核心处理模块
├── utils/                  # 工具模块
├── static/
│   ├── uploads/           # 用户上传的文件
│   └── output/            # 生成的词典文件
├── templates/             # HTML模板
├── logs/                  # 应用日志
├── deploy/                # 部署脚本
├── AutoMdxBuilder         # 词典生成工具
├── docker-compose.yml     # Docker编排
├── Dockerfile             # Docker镜像
├── nginx.conf             # Nginx配置
└── .env                   # 环境变量
```

---

## 运维管理

### 日常管理命令

```bash
# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f                    # 所有服务
docker-compose logs -f web                # Web服务
docker-compose logs -f celery_worker      # Celery Worker

# 重启服务
docker-compose restart
docker-compose restart web                # 重启单个服务

# 停止服务
docker-compose down

# 启动服务
docker-compose up -d

# 重新构建并启动
docker-compose up -d --build

# 进入容器
docker-compose exec web bash
docker-compose exec celery_worker bash
```

### 更新应用

```bash
cd /opt/automdxbuilder

# 拉取最新代码
git pull origin main

# 重新构建并重启
docker-compose down
docker-compose build
docker-compose up -d

# 查看日志确认
docker-compose logs -f
```

### 数据备份

```bash
# 备份脚本
#!/bin/bash
BACKUP_DIR="/backup/automdxbuilder/$(date +%Y%m%d)"
mkdir -p $BACKUP_DIR

# 备份用户数据
tar -czf $BACKUP_DIR/uploads.tar.gz /opt/automdxbuilder/static/uploads/
tar -czf $BACKUP_DIR/output.tar.gz /opt/automdxbuilder/static/output/

# 备份配置
cp /opt/automdxbuilder/.env $BACKUP_DIR/

# 备份Redis数据
docker-compose exec redis redis-cli SAVE
cp /var/lib/docker/volumes/automdxbuilder_web_redis_data/_data/dump.rdb $BACKUP_DIR/

echo "备份完成: $BACKUP_DIR"
```

保存为 `/opt/backup_automdx.sh` 并添加定时任务：
```bash
chmod +x /opt/backup_automdx.sh
crontab -e
# 每天凌晨2点备份
0 2 * * * /opt/backup_automdx.sh
```

### 清理旧文件

```bash
# 手动清理超过24小时的文件
docker-compose exec web python -c "
from app.tasks import cleanup_old_files
cleanup_old_files.delay(24)
"

# 或配置定时任务（已在Celery Beat中配置）
```

### 监控

#### 系统监控
```bash
# 查看系统资源
htop

# 查看磁盘使用
df -h

# 查看Docker资源使用
docker stats
```

#### 应用监控
```bash
# 查看Celery任务
docker-compose exec celery_worker celery -A app.tasks.celery_app inspect active

# 查看Redis状态
docker-compose exec redis redis-cli INFO
```

---

## HTTPS配置（可选但推荐）

### 使用Let's Encrypt免费证书

#### 1. 安装Certbot
```bash
apt-get install certbot
```

#### 2. 获取证书
```bash
# 停止Nginx
docker-compose stop nginx

# 获取证书
certbot certonly --standalone -d your-domain.com

# 证书位置
ls /etc/letsencrypt/live/your-domain.com/
```

#### 3. 配置Nginx
```bash
# 创建SSL目录
mkdir -p /opt/automdxbuilder/ssl

# 复制证书
cp /etc/letsencrypt/live/your-domain.com/fullchain.pem /opt/automdxbuilder/ssl/cert.pem
cp /etc/letsencrypt/live/your-domain.com/privkey.pem /opt/automdxbuilder/ssl/key.pem
```

#### 4. 修改nginx.conf
取消nginx.conf中HTTPS部分的注释，然后重启：
```bash
docker-compose restart nginx
```

#### 5. 自动续期
```bash
# 添加定时任务
crontab -e

# 每月1号凌晨2点续期
0 2 1 * * certbot renew --quiet && cp /etc/letsencrypt/live/your-domain.com/*.pem /opt/automdxbuilder/ssl/ && docker-compose restart nginx
```

---

## 性能优化

### 1. Nginx优化
在nginx.conf中调整：
```nginx
worker_processes auto;
worker_connections 2048;
client_max_body_size 1G;  # 如果需要处理大文件
```

### 2. Gunicorn优化
在docker-compose.yml中调整：
```yaml
command: gunicorn --bind 0.0.0.0:8000 --workers 8 --timeout 600 wsgi:app
# workers数量 = (2 x CPU核心数) + 1
```

### 3. Celery优化
```yaml
command: celery -A app.tasks.celery_app worker --loglevel=info --concurrency=4
# concurrency = CPU核心数
```

### 4. Redis优化
```yaml
command: redis-server --appendonly yes --maxmemory 2gb --maxmemory-policy allkeys-lru
```

---

## 常见问题

### 1. 容器启动失败
```bash
# 查看详细日志
docker-compose logs web

# 常见原因：
# - 端口被占用：修改docker-compose.yml中的端口
# - AutoMdxBuilder路径错误：检查.env配置
# - 权限问题：sudo chown -R $USER:$USER /opt/automdxbuilder
```

### 2. 上传文件失败
```bash
# 检查磁盘空间
df -h

# 检查Nginx配置
docker-compose exec nginx nginx -t

# 增加上传大小限制（nginx.conf）
client_max_body_size 1G;
```

### 3. PDF转换慢
```bash
# 增加Celery worker数量
docker-compose up -d --scale celery_worker=4

# 或修改docker-compose.yml中的concurrency
```

### 4. 内存不足
```bash
# 查看内存使用
free -h

# 限制容器内存
# 在docker-compose.yml中添加：
services:
  web:
    mem_limit: 2g
  celery_worker:
    mem_limit: 4g
```

### 5. 文件清理
```bash
# 手动清理
docker-compose exec web python -c "
from pathlib import Path
import shutil
for d in Path('/app/static/uploads').iterdir():
    if d.is_dir():
        shutil.rmtree(d)
"
```

---

## 安全建议

1. **修改默认端口**
   - SSH端口改为非标准端口
   - 只允许特定IP访问SSH

2. **配置防火墙**
   ```bash
   ufw enable
   ufw allow 22/tcp
   ufw allow 80/tcp
   ufw allow 443/tcp
   ```

3. **定期更新**
   ```bash
   apt-get update && apt-get upgrade -y
   docker-compose pull
   docker-compose up -d
   ```

4. **限制文件大小和数量**
   - 在config.py中设置合理的限制
   - 配置自动清理任务

5. **使用HTTPS**
   - 强制HTTPS访问
   - 配置HSTS头

---

## 技术支持

- **项目文档**: [README.md](README.md)
- **问题反馈**: [GitHub Issues](https://github.com/yourusername/AutoMdxBuilder_Web/issues)
- **邮件**: your-email@example.com

---

## 附录

### A. 完整部署清单

- [ ] 购买并配置阿里云ECS
- [ ] 配置安全组规则
- [ ] 上传项目文件
- [ ] 上传AutoMdxBuilder可执行文件
- [ ] 运行setup_server.sh
- [ ] 配置.env文件
- [ ] 运行aliyun_deploy.sh
- [ ] 测试访问
- [ ] 配置HTTPS（可选）
- [ ] 配置备份任务
- [ ] 配置监控告警

### B. 端口使用说明

```
80    - HTTP访问（Nginx）
443   - HTTPS访问（Nginx）
8000  - Flask应用（内部）
6379  - Redis（内部）
```

### C. 资源估算

| 用户数 | CPU | 内存 | 硬盘 | 带宽 |
|--------|-----|------|------|------|
| <10    | 2核 | 4GB  | 40GB | 5M   |
| 10-50  | 4核 | 8GB  | 100GB| 10M  |
| 50-200 | 8核 | 16GB | 200GB| 20M  |
| >200   | 16核| 32GB | 500GB| 50M  |

---

**部署完成后，你的AutoMdxBuilder Web应用就可以在阿里云上运行了！**
