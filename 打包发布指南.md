# AutoMdxBuilder 桌面应用打包发布指南

本文档记录了如何将 Python Web 应用打包成可以"一键运行"的桌面程序，让不懂技术的用户也能直接使用。

---

## 背景问题

原始项目是一个 **Flask + Celery + Redis** 架构的 Web 应用：
- Flask：Web 服务
- Celery：异步任务队列
- Redis：消息中间件

这种架构对于服务器部署很好，但对于桌面用户来说太复杂——需要安装 Python、Redis、启动多个服务，普通用户根本无法操作。

---

## 解决方案

### 核心思路：简化架构

用 **Python 线程（threading）** 替代 Celery，用 **内存字典** 替代 Redis，这样就不需要任何外部依赖。

| 原架构 | 简化后 |
|--------|--------|
| Celery 异步任务 | threading.Thread |
| Redis 消息队列 | Python 字典存储任务状态 |
| 需要启动 3 个服务 | 只需双击一个程序 |

### 创建的简化版文件

```
app_simple/
├── __init__.py      # Flask 应用工厂
├── routes.py        # API 路由
└── task_manager.py  # 线程任务管理器（替代 Celery）

desktop_simple.py    # 桌面启动器入口
```

---

## 使用的工具

### 1. PyInstaller
将 Python 程序打包成独立可执行文件的工具。

```bash
pip install pyinstaller
```

### 2. GitHub Actions
GitHub 提供的免费 CI/CD 服务，可以在云端自动构建 Mac 和 Windows 版本。

---

## 打包配置文件

### PyInstaller 配置：`AutoMdxBuilder_Simple.spec`

```python
# -*- mode: python ; coding: utf-8 -*-
added_files = [
    ('static', 'static'),
    ('templates', 'templates'),
    ('app_simple', 'app_simple'),
    ('core', 'core'),
    ('utils', 'utils'),
    ('AutoMdxBuilder_python', 'AutoMdxBuilder_python'),
]

a = Analysis(
    ['desktop_simple.py'],  # 入口文件
    datas=added_files,
    hiddenimports=[
        'flask', 'flask_cors', 'fitz', 'PIL', 'openpyxl',
        # ... 其他隐藏导入
    ],
    excludes=[
        'celery', 'redis', 'kombu',  # 排除不需要的大型依赖
    ],
)

# macOS 打包为 .app
if sys.platform == 'darwin':
    app = BUNDLE(coll, name='AutoMdxBuilder.app')
```

### GitHub Actions 配置：`.github/workflows/build.yml`

```yaml
name: Build Desktop App

on:
  push:
    branches: [main]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            artifact_name: AutoMdxBuilder-Mac
          - os: windows-latest
            artifact_name: AutoMdxBuilder-Windows

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pyinstaller flask flask-cors PyMuPDF Pillow openpyxl python-dotenv

      - name: Build
        run: pyinstaller --noconfirm AutoMdxBuilder_Simple.spec

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: release/
```

---

## 本地打包方法

如果只需要在本地打包（比如只打包 Mac 版），可以直接运行：

```bash
# 进入项目目录
cd /Users/lizhouyuan/PycharmProjects/深度學習/11\ 電子辭典製作/AutoMdxBuilder_Web

# 激活虚拟环境
source venv/bin/activate

# 安装依赖
pip install pyinstaller

# 执行打包
pyinstaller --noconfirm AutoMdxBuilder_Simple.spec

# 打包结果在 dist/ 目录
# Mac: dist/AutoMdxBuilder.app
# Windows: dist/AutoMdxBuilder/AutoMdxBuilder.exe
```

或者使用打包脚本：

```bash
python build_simple.py
```

---

## 云端自动打包（推荐）

使用 GitHub Actions 可以同时构建 Mac 和 Windows 版本，无需拥有两台电脑。

### 工作流程

1. 修改代码
2. 提交并推送到 GitHub
   ```bash
   git add .
   git commit -m "更新说明"
   git push
   ```
3. GitHub 自动开始构建（约 1-2 分钟）
4. 构建完成后下载

---

## 下载打包好的程序

### 下载地址

**GitHub Actions 构建产物：**
https://github.com/anon-research-tools/AutoMdxBuilder-Web/actions

点击最新的成功构建（绿色勾），往下滚动到 **Artifacts** 部分：
- `AutoMdxBuilder-Mac` - Mac 版本
- `AutoMdxBuilder-Windows` - Windows 版本

### 直接链接（最新构建）

https://github.com/anon-research-tools/AutoMdxBuilder-Web/actions/runs/20740986748

---

## 更新程序

### 开发者更新流程

1. 修改代码
2. 提交推送：
   ```bash
   git add .
   git commit -m "修复xxx问题"
   git push
   ```
3. 等待 GitHub Actions 构建完成（约 1-2 分钟）
4. 下载新版本分发给用户

### 用户更新流程

1. 删除旧版本
2. 下载新版本
3. 解压后直接使用

---

## 使用说明

### Mac 用户

1. 下载 `AutoMdxBuilder-Mac.zip`
2. 解压得到 `AutoMdxBuilder.app`
3. 双击运行
4. 首次打开可能提示"无法验证开发者"，需要：
   - 打开「系统设置」→「隐私与安全性」
   - 找到 AutoMdxBuilder，点击「仍要打开」

### Windows 用户

1. 下载 `AutoMdxBuilder-Windows.zip`
2. 解压得到 `AutoMdxBuilder` 文件夹
3. 进入文件夹，双击 `AutoMdxBuilder.exe`
4. 如果提示 Windows 保护，点击「更多信息」→「仍要运行」

### 通用说明

- 程序启动后会自动打开浏览器
- 在浏览器中进行操作
- **不要关闭黑色窗口（终端/命令行）**，关闭会停止服务
- 生成的文件保存在程序同目录的 `static` 文件夹中

---

## 项目结构说明

```
AutoMdxBuilder_Web/
├── app_simple/              # 简化版应用（用于打包）
│   ├── __init__.py
│   ├── routes.py
│   └── task_manager.py
├── desktop_simple.py        # 桌面启动入口
├── AutoMdxBuilder_Simple.spec  # PyInstaller 配置
├── build_simple.py          # 本地打包脚本
├── .github/workflows/
│   └── build.yml            # GitHub Actions 配置
├── app/                     # 原版应用（需要 Celery/Redis）
├── core/                    # 核心处理模块
├── utils/                   # 工具模块
├── templates/               # HTML 模板
└── static/                  # 静态文件
```

---

## 问题修复案例：Windows 跨平台路径错误

### 问题描述

**报错信息：**
```
2026-01-06 21:27:51 - AutoMdxBuilder_GUI - ERROR - 找不到 AutoMdxBuilder: \usr\local\bin\AutoMdxBuilder
```

**用户反馈：** Windows 用户无法使用 MDX 构建功能，提示找不到 AutoMdxBuilder 可执行文件。

### 问题诊断

#### 1. 定位问题代码

通过搜索错误信息，找到问题出在 `app_simple/task_manager.py:225` 行：

```python
# ❌ 错误的代码
amb_path = os.environ.get('AMB_PATH', '/usr/local/bin/AutoMdxBuilder')
```

**问题原因：**
- 代码使用硬编码的 Unix 路径 `/usr/local/bin/AutoMdxBuilder`
- 这个路径在 Windows 上完全无效（Windows 没有 `/usr/local/bin/` 目录）
- 打包的程序已经包含了 AutoMdxBuilder，但代码没有检测打包环境

#### 2. 验证打包文件

检查打包后的程序结构：

```bash
# Mac 版本
dist/AutoMdxBuilder.app/Contents/Resources/AutoMdxBuilder_python/AutoMdxBuilder

# Windows 版本
dist/AutoMdxBuilder/AutoMdxBuilder_python/AutoMdxBuilder
```

确认 AutoMdxBuilder 已经被打包进去，但代码没有使用它。

### 解决方案

#### 修改 `app_simple/task_manager.py`

**1. 添加必要的 import：**

```python
import os
import sys
import platform
```

**2. 创建智能路径查找函数：**

```python
def _find_automdxbuilder_path() -> str:
    """
    智能查找 AutoMdxBuilder 可执行文件路径

    优先级:
    1. 环境变量 AMB_PATH
    2. 打包后的程序内置路径
    3. 当前目录下的 AutoMdxBuilder_python/AutoMdxBuilder
    4. 系统默认路径（根据操作系统）
    """
    # 1. 检查环境变量
    if 'AMB_PATH' in os.environ:
        return os.environ['AMB_PATH']

    # 2. 检测是否在 PyInstaller 打包环境中
    if getattr(sys, 'frozen', False):
        # 打包环境：AutoMdxBuilder 在资源目录中
        if hasattr(sys, '_MEIPASS'):
            # sys._MEIPASS 是 PyInstaller 解压临时文件的目录
            bundled_path = Path(sys._MEIPASS) / 'AutoMdxBuilder_python' / 'AutoMdxBuilder'
            if bundled_path.exists():
                logger.info(f"使用打包的 AutoMdxBuilder: {bundled_path}")
                return str(bundled_path)

    # 3. 检查当前目录
    current_dir = Path.cwd()
    local_path = current_dir / 'AutoMdxBuilder_python' / 'AutoMdxBuilder'
    if local_path.exists():
        logger.info(f"使用本地 AutoMdxBuilder: {local_path}")
        return str(local_path)

    # 4. 根据操作系统返回默认路径
    system = platform.system()
    if system == 'Windows':
        # Windows 默认路径
        default_paths = [
            r'C:\Program Files\AutoMdxBuilder\AutoMdxBuilder.exe',
            r'C:\AutoMdxBuilder\AutoMdxBuilder.exe',
            'AutoMdxBuilder.exe'
        ]
        for path in default_paths:
            if Path(path).exists():
                logger.info(f"使用系统路径: {path}")
                return path
        return default_paths[0]
    else:
        # macOS/Linux 默认路径
        default_path = '/usr/local/bin/AutoMdxBuilder'
        logger.info(f"使用默认路径: {default_path}")
        return default_path
```

**3. 更新调用代码：**

```python
# ✅ 修复后的代码
def build_mdx_task(task_id: str, config_path: str, output_dir: str) -> dict:
    """MDX 构建任务"""
    try:
        # ... 省略其他代码 ...

        # 智能查找 AutoMdxBuilder 路径
        amb_path = _find_automdxbuilder_path()
        executor = AutoMdxBuilderExecutor(amb_path)

        # ... 省略其他代码 ...
```

### 提交和发布流程

#### 1. 提交修改到 Git

```bash
# 添加修改的文件
git add app_simple/task_manager.py

# 提交修改
git commit -m "修复 Windows 跨平台路径问题

问题：
- Windows 用户报告'找不到 AutoMdxBuilder: \usr\local\bin\AutoMdxBuilder'
- 代码使用硬编码的 Unix 路径，在 Windows 上无效

解决方案：
- 添加智能路径查找函数 _find_automdxbuilder_path()
- 自动检测打包环境并使用内置的 AutoMdxBuilder
- 根据操作系统使用不同的默认路径
- 优先级：环境变量 > 打包路径 > 本地路径 > 系统默认路径

影响文件：
- app_simple/task_manager.py"
```

#### 2. 推送到 GitHub

```bash
git push
```

#### 3. 查看自动构建状态

```bash
# 查看最近的构建
gh run list --limit 3

# 查看构建详情
gh run view <run-id> --web
```

或访问：https://github.com/anon-research-tools/AutoMdxBuilder-Web/actions

#### 4. 下载修复后的版本

构建完成后（约 1-2 分钟），在 Actions 页面下载 Artifacts：
- `AutoMdxBuilder-Windows.zip` - 修复后的 Windows 版本
- `AutoMdxBuilder-Mac.zip` - 修复后的 Mac 版本

### Git 工作原理：为什么只推送很小的数据？

#### 核心概念：增量传输

Git 是一个**版本控制系统**，它的核心优势是**只传输变化的内容**，而不是整个项目。

#### 形象比喻

**传统方式（比如发邮件）：**
```
📦 每次都发送整个项目文件夹 (1.5 GB)
❌ 修改 1 行代码也要传输全部文件
```

**Git 方式：**
```
📝 只记录和传输"变化"(几 KB)
✅ 就像发送"修改说明书"而不是整本书
```

#### 实际数据对比

本次修复的实际数据：

```bash
# 查看修改统计
$ git show --stat 44e89e5

1 file changed, 58 insertions(+), 1 deletion(-)
```

**流量对比：**

| 项目 | 大小 | 说明 |
|------|------|------|
| 项目总大小 | 1.5 GB | 包含所有文件、依赖、测试数据 |
| Git 仓库大小 | 190 MB | Git 压缩后的完整历史 |
| **本次推送数据** | **约 3 KB** | 只包含 58 行新代码 + 1 行删除 |

**节省比例：** 3 KB / 1500 MB = 0.0002% → **节省了 50 万倍的流量！** 🎉

#### Git 的三大技术优势

##### 1️⃣ 增量传输（Delta Compression）

```
不发送：整个 task_manager.py 文件 (261 行)
只发送：+ 新增的 58 行代码
        - 删除的 1 行旧代码
```

##### 2️⃣ 智能压缩

Git 会对传输的数据进行压缩，进一步减小体积。

##### 3️⃣ 精确对比

Git 能精确知道：
- 哪些文件变了（只有 1 个文件）
- 哪些文件没变（其他所有文件）
- 具体改了什么（58 行新增，1 行删除）

#### 通俗解释

假设你在编辑一本 500 页的书：

**方法 A：不用 Git**
```
📚 修改了第 50 页的一个错别字
📦 把整本 500 页的书重新打印并寄给出版社
💰 运费：$50
```

**方法 B：用 Git**
```
📝 修改了第 50 页的一个错别字
✉️  只发一张便签："第 50 页第 3 行，'的'改成'地'"
💰 运费：$0.50（一张邮票）
```

#### GitHub Actions 的额外好处

使用 GitHub Actions 云端构建还有个巨大优势：

```
传统方式：
你的 Mac 电脑 → 只能打包 Mac 版
              → Windows 版需要另一台 Windows 电脑

GitHub Actions 方式：
推送 3 KB 代码 → GitHub 云端同时构建
                → Mac 版 + Windows 版 都有了！
                → 无需两台电脑
```

#### 实际操作示例

**查看项目大小：**

```bash
# 项目总大小
$ du -sh .
1.5G    .

# Git 仓库大小
$ du -sh .git
190M    .git

# 本次修改的实际字节数
$ git diff HEAD^..HEAD | wc -c
3068
```

**查看修改内容：**

```bash
# 查看简洁统计
$ git show --shortstat
1 file changed, 58 insertions(+), 1 deletion(-)

# 查看详细统计
$ git show --stat
app_simple/task_manager.py | 59 +++++++++++++++++++++++++++++++++++++++++
1 file changed, 58 insertions(+), 1 deletion(-)
```

### 关键要点总结

1. **跨平台开发的注意事项：**
   - 避免硬编码路径
   - 使用 `platform.system()` 检测操作系统
   - 使用 `Path` 对象处理路径（自动处理 `/` 和 `\`）
   - 检测 PyInstaller 打包环境 `sys.frozen` 和 `sys._MEIPASS`

2. **Git 的高效性：**
   - 只传输变化，不传输整个项目
   - 自动压缩数据
   - 精确追踪每一行代码的改动
   - 适合多人协作和频繁更新

3. **GitHub Actions 的优势：**
   - 无需本地打包，云端自动构建
   - 同时支持多个平台（Mac、Windows、Linux）
   - 每次推送自动触发，1-2 分钟完成
   - 构建产物可直接下载分发

4. **问题修复流程：**
   ```
   发现问题 → 诊断定位 → 编写修复代码 → 测试验证
              ↓
   提交到 Git → 推送到 GitHub → 自动构建 → 下载分发
   ```

---

## 常见问题

### Q: 打包后的程序体积很大？
A: 正常现象。PyInstaller 会把 Python 解释器和所有依赖打包进去，通常在 300-500MB。

### Q: Mac 上提示"已损坏，无法打开"？
A: 在终端运行：
```bash
xattr -cr /path/to/AutoMdxBuilder.app
```

### Q: Windows 杀毒软件报警？
A: PyInstaller 打包的程序经常被误报。可以添加到白名单。

### Q: 如何查看构建日志？
A: 在 GitHub Actions 页面点击具体的构建任务，可以看到详细日志。

---

## 相关链接

- **GitHub 仓库**: https://github.com/anon-research-tools/AutoMdxBuilder-Web
- **Actions 构建页**: https://github.com/anon-research-tools/AutoMdxBuilder-Web/actions
- **PyInstaller 文档**: https://pyinstaller.org/
- **GitHub Actions 文档**: https://docs.github.com/en/actions

---

*最后更新: 2026-01-06*
