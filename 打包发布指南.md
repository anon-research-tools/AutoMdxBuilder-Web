# AutoMdxBuilder 桌面应用打包发布指南

本文档记录了如何将 Python Web 应用打包成可以"一键运行"的桌面程序，让不懂技术的用户也能直接使用。

---

## 背景问题

原始项目是一个 **Flask + Celery + Redis** 架构的 Web 应用：
- Flask：Web 服务
- Celery：异步任务队列
- Redis：消息中间件

这种架构对于服务器部署很好，但对于桌面用户来说太复杂——需要安装 Python、Redis、启动多个服务，普通用户根本无法操作。

---

## 解决方案

### 核心思路：简化架构

用 **Python 线程（threading）** 替代 Celery，用 **内存字典** 替代 Redis，这样就不需要任何外部依赖。

| 原架构 | 简化后 |
|--------|--------|
| Celery 异步任务 | threading.Thread |
| Redis 消息队列 | Python 字典存储任务状态 |
| 需要启动 3 个服务 | 只需双击一个程序 |

### 创建的简化版文件

```
app_simple/
├── __init__.py      # Flask 应用工厂
├── routes.py        # API 路由
└── task_manager.py  # 线程任务管理器（替代 Celery）

desktop_simple.py    # 桌面启动器入口
```

---

## 使用的工具

### 1. PyInstaller
将 Python 程序打包成独立可执行文件的工具。

```bash
pip install pyinstaller
```

### 2. GitHub Actions
GitHub 提供的免费 CI/CD 服务，可以在云端自动构建 Mac 和 Windows 版本。

---

## 打包配置文件

### PyInstaller 配置：`AutoMdxBuilder_Simple.spec`

```python
# -*- mode: python ; coding: utf-8 -*-
added_files = [
    ('static', 'static'),
    ('templates', 'templates'),
    ('app_simple', 'app_simple'),
    ('core', 'core'),
    ('utils', 'utils'),
    ('AutoMdxBuilder_python', 'AutoMdxBuilder_python'),
]

a = Analysis(
    ['desktop_simple.py'],  # 入口文件
    datas=added_files,
    hiddenimports=[
        'flask', 'flask_cors', 'fitz', 'PIL', 'openpyxl',
        # ... 其他隐藏导入
    ],
    excludes=[
        'celery', 'redis', 'kombu',  # 排除不需要的大型依赖
    ],
)

# macOS 打包为 .app
if sys.platform == 'darwin':
    app = BUNDLE(coll, name='AutoMdxBuilder.app')
```

### GitHub Actions 配置：`.github/workflows/build.yml`

```yaml
name: Build Desktop App

on:
  push:
    branches: [main]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            artifact_name: AutoMdxBuilder-Mac
          - os: windows-latest
            artifact_name: AutoMdxBuilder-Windows

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pyinstaller flask flask-cors PyMuPDF Pillow openpyxl python-dotenv

      - name: Build
        run: pyinstaller --noconfirm AutoMdxBuilder_Simple.spec

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: release/
```

---

## 本地打包方法

如果只需要在本地打包（比如只打包 Mac 版），可以直接运行：

```bash
# 进入项目目录
cd /Users/lizhouyuan/PycharmProjects/深度學習/11\ 電子辭典製作/AutoMdxBuilder_Web

# 激活虚拟环境
source venv/bin/activate

# 安装依赖
pip install pyinstaller

# 执行打包
pyinstaller --noconfirm AutoMdxBuilder_Simple.spec

# 打包结果在 dist/ 目录
# Mac: dist/AutoMdxBuilder.app
# Windows: dist/AutoMdxBuilder/AutoMdxBuilder.exe
```

或者使用打包脚本：

```bash
python build_simple.py
```

---

## 云端自动打包（推荐）

使用 GitHub Actions 可以同时构建 Mac 和 Windows 版本，无需拥有两台电脑。

### 工作流程

1. 修改代码
2. 提交并推送到 GitHub
   ```bash
   git add .
   git commit -m "更新说明"
   git push
   ```
3. GitHub 自动开始构建（约 1-2 分钟）
4. 构建完成后下载

---

## 下载打包好的程序

### 下载地址

**GitHub Actions 构建产物：**
https://github.com/anon-research-tools/AutoMdxBuilder-Web/actions

点击最新的成功构建（绿色勾），往下滚动到 **Artifacts** 部分：
- `AutoMdxBuilder-Mac` - Mac 版本
- `AutoMdxBuilder-Windows` - Windows 版本

### 直接链接（最新构建）

https://github.com/anon-research-tools/AutoMdxBuilder-Web/actions/runs/20740986748

---

## 更新程序

### 开发者更新流程

1. 修改代码
2. 提交推送：
   ```bash
   git add .
   git commit -m "修复xxx问题"
   git push
   ```
3. 等待 GitHub Actions 构建完成（约 1-2 分钟）
4. 下载新版本分发给用户

### 用户更新流程

1. 删除旧版本
2. 下载新版本
3. 解压后直接使用

---

## 使用说明

### Mac 用户

1. 下载 `AutoMdxBuilder-Mac.zip`
2. 解压得到 `AutoMdxBuilder.app`
3. 双击运行
4. 首次打开可能提示"无法验证开发者"，需要：
   - 打开「系统设置」→「隐私与安全性」
   - 找到 AutoMdxBuilder，点击「仍要打开」

### Windows 用户

1. 下载 `AutoMdxBuilder-Windows.zip`
2. 解压得到 `AutoMdxBuilder` 文件夹
3. 进入文件夹，双击 `AutoMdxBuilder.exe`
4. 如果提示 Windows 保护，点击「更多信息」→「仍要运行」

### 通用说明

- 程序启动后会自动打开浏览器
- 在浏览器中进行操作
- **不要关闭黑色窗口（终端/命令行）**，关闭会停止服务
- 生成的文件保存在程序同目录的 `static` 文件夹中

---

## 项目结构说明

```
AutoMdxBuilder_Web/
├── app_simple/              # 简化版应用（用于打包）
│   ├── __init__.py
│   ├── routes.py
│   └── task_manager.py
├── desktop_simple.py        # 桌面启动入口
├── AutoMdxBuilder_Simple.spec  # PyInstaller 配置
├── build_simple.py          # 本地打包脚本
├── .github/workflows/
│   └── build.yml            # GitHub Actions 配置
├── app/                     # 原版应用（需要 Celery/Redis）
├── core/                    # 核心处理模块
├── utils/                   # 工具模块
├── templates/               # HTML 模板
└── static/                  # 静态文件
```

---

## 常见问题

### Q: 打包后的程序体积很大？
A: 正常现象。PyInstaller 会把 Python 解释器和所有依赖打包进去，通常在 300-500MB。

### Q: Mac 上提示"已损坏，无法打开"？
A: 在终端运行：
```bash
xattr -cr /path/to/AutoMdxBuilder.app
```

### Q: Windows 杀毒软件报警？
A: PyInstaller 打包的程序经常被误报。可以添加到白名单。

### Q: 如何查看构建日志？
A: 在 GitHub Actions 页面点击具体的构建任务，可以看到详细日志。

---

## 相关链接

- **GitHub 仓库**: https://github.com/anon-research-tools/AutoMdxBuilder-Web
- **Actions 构建页**: https://github.com/anon-research-tools/AutoMdxBuilder-Web/actions
- **PyInstaller 文档**: https://pyinstaller.org/
- **GitHub Actions 文档**: https://docs.github.com/en/actions

---

*最后更新: 2026-01-06*
