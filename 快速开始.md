# AutoMdxBuilder Web - 快速开始

## 🚀 5分钟快速上手

### 前提条件

```bash
# 1. 检查Python版本
python3 --version  # 需要 3.11+

# 2. 安装Redis（如果没有）
# macOS:
brew install redis

# Ubuntu:
sudo apt-get install redis-server
```

---

## 方法1: 一键启动（推荐）

```bash
# 1. 进入项目目录
cd /Users/lizhouyuan/E盘/MDX製作/電子辭典製作/AutoMdxBuilder_Web

# 2. 复制AutoMdxBuilder
cp ../AutoMdxBuilder/AutoMdxBuilder ./AutoMdxBuilder

# 3. 运行一键启动脚本
./启动全部服务.sh

# 4. 访问应用
# http://localhost:8000
```

**就这么简单！** 脚本会自动：
- ✅ 检查并启动Redis
- ✅ 创建虚拟环境
- ✅ 安装依赖
- ✅ 配置环境变量
- ✅ 启动所有服务（Flask + Celery + Beat）

---

## 方法2: Docker部署

```bash
# 1. 确保有Docker
docker --version

# 2. 进入项目目录
cd /Users/lizhouyuan/E盘/MDX製作/電子辭典製作/AutoMdxBuilder_Web

# 3. 复制AutoMdxBuilder
cp ../AutoMdxBuilder/AutoMdxBuilder ./AutoMdxBuilder

# 4. 配置环境变量
cp .env.example .env

# 5. 一键启动
docker-compose up -d

# 6. 查看状态
docker-compose ps

# 7. 访问应用
# http://localhost
```

---

## 方法3: 手动启动

### 准备工作
```bash
cd /Users/lizhouyuan/E盘/MDX製作/電子辭典製作/AutoMdxBuilder_Web

# 复制AutoMdxBuilder
cp ../AutoMdxBuilder/AutoMdxBuilder ./AutoMdxBuilder

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 配置环境
cp .env.example .env
# 编辑.env，设置AMB_PATH为AutoMdxBuilder的绝对路径

# 启动Redis
brew services start redis  # macOS
```

### 启动服务（需要3个终端）

**终端1 - Flask Web**:
```bash
cd /Users/lizhouyuan/E盘/MDX製作/電子辭典製作/AutoMdxBuilder_Web
source venv/bin/activate
python wsgi.py
```

**终端2 - Celery Worker**:
```bash
cd /Users/lizhouyuan/E盘/MDX製作/電子辭典製作/AutoMdxBuilder_Web
source venv/bin/activate
celery -A app.tasks.celery_app worker --loglevel=info
```

**终端3 - Celery Beat** (可选):
```bash
cd /Users/lizhouyuan/E盘/MDX製作/電子辭典製作/AutoMdxBuilder_Web
source venv/bin/activate
celery -A app.tasks.celery_app beat --loglevel=info
```

访问: **http://localhost:8000**

---

## 使用流程

### 1️⃣ 上传PDF
- 打开 http://localhost:8000
- 拖拽或选择PDF文件
- 等待上传完成

### 2️⃣ 配置转换
- DPI: 选择300（推荐）
- 质量: 选择85%（推荐）
- 可选：勾选"自动裁剪"

### 3️⃣ 开始转换
- 点击"开始转换"
- 观看实时进度

### 4️⃣ 配置词典
- 输入词典名称
- 输入作者信息
- 添加描述（可选）

### 5️⃣ 生成并下载
- 点击"生成MDX"
- 等待完成
- 点击"下载词典文件"

---

## 常见命令

### 查看日志
```bash
# 应用日志
tail -f logs/app.log

# Docker日志
docker-compose logs -f
```

### 重启服务
```bash
# Docker
docker-compose restart

# 手动启动
# 在对应终端按Ctrl+C，然后重新运行命令
```

### 停止服务
```bash
# Docker
docker-compose down

# 手动启动
# 在所有终端按Ctrl+C

# tmux启动
tmux kill-session -t automdx_web
```

---

## 故障排查

### Redis连接失败
```bash
# 检查Redis
redis-cli ping

# 启动Redis
brew services start redis  # macOS
sudo systemctl start redis  # Linux
```

### 端口被占用
```bash
# 查找占用8000端口的进程
lsof -i :8000

# 杀死进程
kill -9 <PID>
```

### AutoMdxBuilder未找到
```bash
# 检查文件是否存在
ls -la AutoMdxBuilder

# 检查.env配置
cat .env | grep AMB_PATH

# 设置正确的路径（绝对路径）
```

---

## ⚙️ 性能优化与资源管理

为了确保阿里云服务器（如 4核8G）在多用户使用时的稳定性，我们实施了四层资源控制。如需调整，请参考以下说明：

### 1. 并发封顶（控制任务排队）
- **作用**：限制同时进行的重型任务（如词典构建）数量。
- **调整位置**：`docker-compose.yml`
```yaml
celery_worker:
  command: celery -A app.tasks.celery_app worker --concurrency=1 # 默认为1，1-2 适合 4核服务器
```

### 2. 线程削减（控制单任务负载）
- **作用**：控制 PDF 转换图片时的并行程度。
- **调整位置**：`core/pdf_processor.py`
```python
# 在 _extract_images_parallel 函数中
num_workers = min(cpu_count(), 12) # 建议不要超过物理核心数
```

### 3. 优先级让路（保证网页不卡顿）
- **作用**：使用 `nice` 命令让后台构建进程“礼让” Web 服务。
- **调整位置**：`core/amb_executor.py`
```python
# 代码中已默认添加 nice -n 10
cmd = ['nice', '-n', '10'] + cmd
```

### 4. 资源硬限制（Docker 级防护）
- **作用**：防止某个容器因异常消耗掉宿主机所有资源。
- **调整位置**：`docker-compose.yml`
```yaml
deploy:
  resources:
    limits:
      cpus: '3.0'    # 限制最高占用 3 核
      memory: 4G    # 限制最高 4GB 内存
```

---

## 📂 文件说明

### 核心文件
- `app/routes.py` - API路由
- `app/tasks.py` - Celery任务
- `templates/index.html` - Web界面
- `config.py` - 配置文件

### 部署文件
- `Dockerfile` - Docker镜像
- `docker-compose.yml` - 容器编排
- `nginx.conf` - Nginx配置

### 文档文件
- `README.md` - 项目说明
- `本地开发指南.md` - 开发文档
- `阿里云部署指南.md` - 部署文档
- `项目完成总结.md` - 项目总结

### 脚本文件
- `启动开发环境.sh` - 环境准备
- `启动全部服务.sh` - 一键启动
- `deploy/setup_server.sh` - 服务器初始化
- `deploy/aliyun_deploy.sh` - 生产部署

---

## 下一步

### 本地测试
1. ✅ 按照"方法1"启动服务
2. ✅ 访问 http://localhost:8000
3. ✅ 上传测试PDF
4. ✅ 完成词典制作流程

### 生产部署
1. 📖 阅读 `阿里云部署指南.md`
2. 🛒 购买阿里云ECS
3. 🚀 运行部署脚本
4. 🌐 配置域名和HTTPS

### 功能扩展
1. 📝 添加用户系统
2. 📊 添加使用统计
3. 💰 添加付费功能
4. 📱 开发移动应用

---

## 获取帮助

**详细文档**:
- [README.md](README.md) - 完整项目说明
- [本地开发指南.md](本地开发指南.md) - 开发者文档
- [阿里云部署指南.md](阿里云部署指南.md) - 部署指南

**问题反馈**:
- GitHub Issues
- 电子邮件

---

**就这么简单！开始制作你的第一个MDX词典吧！** 🎉
