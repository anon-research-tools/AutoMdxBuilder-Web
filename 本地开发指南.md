# 本地开发指南

本文档详细介绍如何在本地搭建 AutoMdxBuilder Web 开发环境。

---

## 环境要求

### 必需软件

- **Python**: 3.11 或更高
- **Redis**: 7.0 或更高
- **Git**: 最新版本

### 可选软件

- **Docker**: 用于容器化部署测试
- **VS Code**: 推荐的代码编辑器

---

## 第一次设置

### 1. 克隆项目

```bash
# 克隆仓库
git clone https://github.com/yourusername/AutoMdxBuilder_Web.git
cd AutoMdxBuilder_Web
```

### 2. 创建虚拟环境

**macOS/Linux**:
```bash
python3 -m venv venv
source venv/bin/activate
```

**Windows**:
```cmd
python -m venv venv
venv\Scripts\activate
```

### 3. 安装依赖

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. 安装 Redis

**macOS**:
```bash
brew install redis
brew services start redis

# 验证安装
redis-cli ping
# 应该返回: PONG
```

**Ubuntu/Debian**:
```bash
sudo apt-get update
sudo apt-get install redis-server
sudo systemctl start redis
sudo systemctl enable redis

# 验证安装
redis-cli ping
```

**Windows**:
1. 下载 [Redis for Windows](https://github.com/microsoftarchive/redis/releases)
2. 解压并运行 `redis-server.exe`

### 5. 准备 AutoMdxBuilder

```bash
# 从桌面版复制 AutoMdxBuilder
cp /Users/lizhouyuan/E盘/MDX製作/AutoMdxBuilder/AutoMdxBuilder ./AutoMdxBuilder

# 设置可执行权限
chmod +x ./AutoMdxBuilder

# 验证
./AutoMdxBuilder --version
```

### 6. 配置环境变量

```bash
# 复制示例配置
cp .env.example .env

# 编辑配置文件
nano .env
```

修改 `.env`:
```bash
FLASK_ENV=development
SECRET_KEY=dev-secret-key
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
AMB_PATH=/绝对路径/到/AutoMdxBuilder_Web/AutoMdxBuilder
FILE_RETENTION_HOURS=24
```

### 7. 创建必要的目录

```bash
mkdir -p static/uploads static/output logs
```

---

## 启动开发服务器

你需要在**3个终端窗口**中分别运行以下服务：

### 终端 1: Flask Web 应用

```bash
cd /path/to/AutoMdxBuilder_Web
source venv/bin/activate  # Windows: venv\Scripts\activate

# 方式 1: 直接运行
python wsgi.py

# 方式 2: 使用 Flask CLI (开发模式)
export FLASK_APP=wsgi.py
export FLASK_ENV=development
flask run --host=0.0.0.0 --port=8000 --reload
```

访问: http://localhost:8000

### 终端 2: Celery Worker

```bash
cd /path/to/AutoMdxBuilder_Web
source venv/bin/activate

celery -A app.tasks.celery_app worker --loglevel=info --concurrency=2
```

**Windows** 用户需要使用:
```cmd
celery -A app.tasks.celery_app worker --loglevel=info --pool=solo
```

### 终端 3: Celery Beat (可选 - 定时任务)

```bash
cd /path/to/AutoMdxBuilder_Web
source venv/bin/activate

celery -A app.tasks.celery_app beat --loglevel=info
```

---

## 开发工作流

### 代码修改流程

1. **修改代码**
   - Flask 代码修改后自动重载（开发模式）
   - Celery 代码修改需要重启 worker

2. **重启 Celery Worker**
   ```bash
   # 在终端 2 中按 Ctrl+C 停止，然后重新运行
   celery -A app.tasks.celery_app worker --loglevel=info
   ```

3. **测试功能**
   - 访问 http://localhost:8000
   - 上传测试 PDF
   - 查看日志输出

### 查看日志

**Flask 日志**:
```bash
# 终端 1 会直接显示日志
# 或查看日志文件
tail -f logs/app.log
```

**Celery 日志**:
```bash
# 终端 2 会直接显示日志
```

**Redis 监控**:
```bash
# 查看 Redis 状态
redis-cli INFO

# 监控 Redis 命令
redis-cli MONITOR

# 查看队列
redis-cli LLEN celery
```

---

## 调试技巧

### 1. Flask 调试模式

在 `.env` 中设置:
```bash
FLASK_ENV=development
```

这会启用：
- 自动重载
- 详细错误页面
- 调试工具栏

### 2. Python 调试器

在代码中添加断点：
```python
import pdb; pdb.set_trace()
```

或使用 VS Code 的调试功能：
```json
// .vscode/launch.json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Flask",
            "type": "python",
            "request": "launch",
            "module": "flask",
            "env": {
                "FLASK_APP": "wsgi.py",
                "FLASK_ENV": "development"
            },
            "args": [
                "run",
                "--host=0.0.0.0",
                "--port=8000"
            ]
        }
    ]
}
```

### 3. Celery 调试

查看 Celery 任务状态：
```python
from app.tasks import celery_app

# 查看活动任务
celery_app.control.inspect().active()

# 查看已注册任务
celery_app.control.inspect().registered()

# 查看统计信息
celery_app.control.inspect().stats()
```

### 4. 测试 API

使用 `curl` 测试 API：

**上传文件**:
```bash
curl -X POST \
  http://localhost:8000/api/upload \
  -F "file=@/path/to/test.pdf"
```

**转换 PDF**:
```bash
curl -X POST \
  http://localhost:8000/api/convert \
  -H "Content-Type: application/json" \
  -d '{
    "task_id": "your-task-id",
    "dpi": 300,
    "quality": 85,
    "crop": false
  }'
```

**查询状态**:
```bash
curl http://localhost:8000/api/status/celery-task-id
```

---

## 数据库管理（如果使用）

目前项目使用 Redis 作为缓存和队列，不需要传统数据库。

如果将来需要添加数据库：

### 安装 SQLAlchemy
```bash
pip install flask-sqlalchemy
```

### 迁移工具
```bash
pip install flask-migrate
```

---

## 测试

### 编写测试

在 `tests/` 目录创建测试文件：

```python
# tests/test_routes.py
import pytest
from app import create_app

@pytest.fixture
def client():
    app = create_app('testing')
    with app.test_client() as client:
        yield client

def test_index(client):
    response = client.get('/')
    assert response.status_code == 200
```

### 运行测试

```bash
# 安装 pytest
pip install pytest pytest-cov

# 运行所有测试
pytest

# 运行特定测试
pytest tests/test_routes.py

# 查看覆盖率
pytest --cov=app tests/
```

---

## 性能分析

### Flask 性能分析

```python
from werkzeug.middleware.profiler import ProfilerMiddleware

app.wsgi_app = ProfilerMiddleware(app.wsgi_app)
```

### Celery 性能监控

安装 Flower（Celery 监控工具）：
```bash
pip install flower

# 启动 Flower
celery -A app.tasks.celery_app flower --port=5555
```

访问: http://localhost:5555

---

## 常见开发问题

### 1. 端口被占用

**问题**: `Address already in use`

**解决**:
```bash
# 查找占用端口的进程
lsof -i :8000

# 杀死进程
kill -9 <PID>

# 或更换端口
flask run --port=8001
```

### 2. Redis 连接失败

**问题**: `ConnectionRefusedError: [Errno 61] Connection refused`

**解决**:
```bash
# 检查 Redis 是否运行
redis-cli ping

# macOS 启动 Redis
brew services start redis

# Linux 启动 Redis
sudo systemctl start redis
```

### 3. AutoMdxBuilder 路径错误

**问题**: `FileNotFoundError: AutoMdxBuilder not found`

**解决**:
```bash
# 检查 .env 中的路径
cat .env | grep AMB_PATH

# 使用绝对路径
AMB_PATH=/Users/your-username/AutoMdxBuilder_Web/AutoMdxBuilder

# 确保有执行权限
chmod +x /path/to/AutoMdxBuilder
```

### 4. Celery Worker 不工作

**问题**: 任务一直 PENDING 状态

**解决**:
```bash
# 检查 worker 是否运行
ps aux | grep celery

# 检查 Redis 队列
redis-cli LLEN celery

# 重启 worker
# 终端 2: Ctrl+C 后重新运行
celery -A app.tasks.celery_app worker --loglevel=info
```

---

## VS Code 配置

### 推荐扩展

- **Python** (Microsoft)
- **Pylance** (Microsoft)
- **autoDocstring** - 自动生成文档字符串
- **GitLens** - Git 增强
- **Docker** - Docker 支持
- **Thunder Client** - API 测试

### 设置文件

创建 `.vscode/settings.json`:
```json
{
    "python.defaultInterpreterPath": "${workspaceFolder}/venv/bin/python",
    "python.linting.enabled": true,
    "python.linting.pylintEnabled": true,
    "python.formatting.provider": "black",
    "editor.formatOnSave": true,
    "files.exclude": {
        "**/__pycache__": true,
        "**/*.pyc": true
    }
}
```

---

## Git 工作流

### 分支策略

```
main          - 生产环境
├── develop   - 开发环境
    ├── feature/xxx  - 新功能
    ├── bugfix/xxx   - Bug 修复
    └── hotfix/xxx   - 紧急修复
```

### 提交规范

```bash
# 功能
git commit -m "feat: 添加用户认证功能"

# 修复
git commit -m "fix: 修复PDF转换内存泄漏"

# 文档
git commit -m "docs: 更新部署文档"

# 样式
git commit -m "style: 调整前端布局"

# 重构
git commit -m "refactor: 重构PDF处理模块"

# 测试
git commit -m "test: 添加路由测试用例"
```

---

## 下一步

1. **阅读代码**
   - `app/routes.py` - 了解 API 设计
   - `app/tasks.py` - 了解异步任务
   - `templates/index.html` - 了解前端交互

2. **尝试修改**
   - 添加新的 API 端点
   - 修改前端样式
   - 优化转换参数

3. **查看文档**
   - [Flask 文档](https://flask.palletsprojects.com/)
   - [Celery 文档](https://docs.celeryq.dev/)
   - [Bootstrap 文档](https://getbootstrap.com/)

---

## 获取帮助

- **项目文档**: [README.md](README.md)
- **部署文档**: [阿里云部署指南.md](阿里云部署指南.md)
- **问题反馈**: [GitHub Issues](https://github.com/yourusername/AutoMdxBuilder_Web/issues)

---

祝你开发愉快！
